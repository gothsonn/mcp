{
  "mcpServers": {
    "atlassian-task-intake": {
      "url": "https://mcp.atlassian.com/v1/mcp/authv2"
    },
    "azure-devops-task-intake": {
      "command": "zsh",
      "args": [
        "-lc",
        "cred=\"${MCP_CREDENTIAL_FILE:-$PWD/credential_mcp.env}\"; org=\"${AZURE_DEVOPS_ORG:-}\"; if [ -f \"$cred\" ]; then org=\"$(awk -F= '/^AZURE_DEVOPS_ORG=/{print substr($0,index($0,\"=\")+1)}' \"$cred\")\"; fi; test -n \"$org\" && npx -y @azure-devops/mcp \"$org\" -d core work work-items"
      ]
    }
  }
}
