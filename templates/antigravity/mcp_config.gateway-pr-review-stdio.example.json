{
  "mcpServers": {
    "gateway-pr-review": {
      "command": "zsh",
      "args": [
        "-lc",
        "repo=\"${MCP_WORKSPACE:-${PWD}}\"; secrets=\"$repo/credential_mcp.env\"; if [ -f \"$secrets\" ]; then exec docker mcp gateway run --profile antigravity-pr-review --secrets \"$secrets\"; else exec docker mcp gateway run --profile antigravity-pr-review; fi"
      ]
    }
  }
}
